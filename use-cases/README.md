# Use cases (reserved)

Placeholder for a future resource type: team-described scenarios for auditing what an agent or prompt actually does — including detecting RGPD/privacy risk. Not built yet, so this folder has no internal structure defined. It sits alongside `corpora/` and `prompts/` as a peer, not nested under either, since a use case can span more than one corpus.
